/**
 * `/code` – Messestand-Ansicht der Arena: aufgeräumt für Besucher, die hier
 * ihren Bot testen. Keine Level-Auswahl (immer Level 1) und kein
 * Phaser-Physik-Debug-Overlay (siehe `.features/code-station-page/`).
 */
import { ArenaPage } from "./ArenaPage";

export function CodePage() {
  return <ArenaPage />;
}
